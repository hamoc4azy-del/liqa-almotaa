'use client'; import {useState} from 'react'; import Link from 'next/link'; import TopBar from '../../components/TopBar'; import Atmosphere from '../../components/Atmosphere'; import Signature from '../../components/Signature';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000';
export default function Search(){const [q,setQ]=useState('');const [items,setItems]=useState<any[]>([]);async function search(){const r=await fetch(`${API}/api/search?q=${encodeURIComponent(q)}`);const d=await r.json();setItems(d.results||[])}return <main className="shell"><TopBar/><Atmosphere/><Signature/><section className="search-page"><div className="section-title"><h1>ابحث في سجل الذكريات</h1><p>الذكريات العامة فقط تظهر هنا.</p></div><div className="search-box"><input className="input" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="اكتب اسم الشخص..."/><button className="primary" onClick={search}>بحث</button></div>{items.length?<div className="results">{items.map(x=><article className="result" key={x.id}><img src={x.imageUrl} alt=""/><div className="result-body"><small>{new Date(x.deathDate).getFullYear()}</small><h3>{x.personName}</h3><Link href={`/meet/${x.id}`} className="ghost">عرض الذكرى</Link></div></article>)}</div>:<div className="empty">لا توجد نتائج بعد.</div>}</section></main>}
