'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Signature from '../../../components/Signature'; const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [e,setE]=useState('');const router=useRouter();async function go(){const r=await fetch(`${API}/api/admin/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setE(d.error);return}localStorage.setItem('liqaa_admin_token',d.token);router.push('/admin')}return <main className="shell login"><Signature/><div className="login-card"><h1>بوابة الإدارة</h1><p style={{color:'#777'}}>الوصول محمي.</p><div className="field"><label>البريد</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>كلمة المرور</label><input type="password" className="input" value={password} onChange={e=>setPassword(e.target.value)}/></div>{e&&<div className="warning">{e}</div>}<button className="primary" style={{width:'100%',marginTop:15}} onClick={go}>دخول</button></div></main>}
