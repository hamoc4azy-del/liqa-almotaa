'use client'; import {useEffect,useState} from 'react';
export default function Atmosphere(){const [mute,setMute]=useState(true);const [x,setX]=useState(0);useEffect(()=>{const f=(e:MouseEvent)=>setX((e.clientX/window.innerWidth-.5)*12);window.addEventListener('mousemove',f);return()=>window.removeEventListener('mousemove',f)},[]);return <><div className="moon" style={{transform:`translateX(${x}px)`}}/><div className="fog"/><div className="fog two"/><div className="fog three"/><div className="trees"/><div className="graveyard">{Array.from({length:9}).map((_,i)=><div className="grave" key={i}/>)}</div><button className="mute" style={{position:'fixed',top:17,left:20,zIndex:40}} onClick={()=>setMute(!mute)} aria-label="تشغيل أو كتم الصوت">{mute?'صامت':'صوت'}</button><div className="grain"/></>}
