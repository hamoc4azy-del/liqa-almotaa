{
  "name": "liqaa-api-deploy",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/api install && npm --prefix apps/api run build",
    "start": "npm --prefix apps/api run start"
  }
}
